import { ContactView } from "@/components/contact-view";

export const metadata = {
  title: "Contact — Aria's Atelier",
  description: "Enquiries, sourcing requests and private appointments.",
};

export default function ContactPage() {
  return <ContactView />;
}
